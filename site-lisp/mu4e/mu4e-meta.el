;; auto-generated
(defconst mu4e-mu-version "1.2.0"
  "Required mu binary version; mu4e's version must agree with this.")

(defconst mu4e-builddir "/tmp/yaourt-tmp-billxue/aur-mu/src/mu-1.2"
  "Top-level build directory.")

(defconst mu4e-doc-dir "/usr/share/doc/mu"
  "Mu4e's data-dir.")

(provide 'mu4e-meta)
